#pragma once

#include <algorithm>
#include <cassert>
#include <cstdint>
#include <list>
#include <map>
#include <memory>
#include <optional>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>

enum class UnaryOp {  };

enum class BinaryOp {

};

enum class ScalarType {

};

struct Type {
};

struct ConstValue {

};

// variable or constant
struct Var {

};

